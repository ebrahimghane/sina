{
  "name": "sina-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "SINA Smart Search Widget - Phase 1: Autocomplete",
  "scripts": {
    "build": "turbo run build",
    "build:widget": "turbo run build --filter=@sina/widget",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

